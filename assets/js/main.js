const readAll = document.querySelector('[data-button="readAll"]')
const notificationList = document.querySelector('ul.notifications-list')

readAll.addEventListener('click', (e) => {
    e.preventDefault()
    
})